# Router-Summary QA Pipeline Requirements
# Install with: pip install -r requirements_pipeline.txt

# Already in requirements.txt
beautifulsoup4>=4.12
marker-pdf>=1.8
pdfplumber>=0.11
PyMuPDF>=1.24
numpy>=1.26
rank-bm25>=0.2.2
sentence-transformers>=3.0
chromadb>=0.5

# New requirements for pipeline_router_summary
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0  # For model optimization
bitsandbytes>=0.41.0  # For quantization (optional)
peft>=0.4.0  # For parameter-efficient fine-tuning (optional)
pydantic>=2.0.0  # For data validation
tqdm>=4.66.0  # Progress bars
